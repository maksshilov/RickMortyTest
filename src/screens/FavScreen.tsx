import React, { FC } from 'react';
import { View, Text } from 'react-native';

export const FavScreen: FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>FavScreen</Text>
    </View>
  );
};
